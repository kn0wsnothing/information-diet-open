[project]
name = "information-diet-showcase"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["fastapi>=0.115,<1", "uvicorn[standard]>=0.30,<1", "jinja2>=3.1,<4", "python-multipart>=0.0.9,<1"]

[tool.pytest.ini_options]
testpaths = ["tests"]
